spring:
  datasource:
    url: jdbc:mysql://localhost:3306/search_engine_test?useSSL=false&serverTimezone=UTC
    username: root
    password: fwgR5rf!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # update, create, create-drop, validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8080
