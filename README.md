# 🔍 Поисковый движок (Search Engine)

Профессиональная система индексации и полнотекстового поиска с морфологическим анализом русского и английского языков. Проект построен на стеке Java + Spring Boot.

![Java](https://img.shields.io/badge/Java-17-red?style=flat&logo=openjdk&logoColor=white)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.1-green?style=flat&logo=springboot&logoColor=white)
![MySQL](https://img.shields.io/badge/MySQL-8.0-blue?style=flat&logo=mysql&logoColor=white)
![Jsoup](https://img.shields.io/badge/Jsoup-1.16-orange?style=flat&logo=java&logoColor=white)
![Status](https://img.shields.io/badge/Status-Рабочий-brightgreen)

---

## 🌟 Основные возможности
* **Автоматическая индексация:** Сканирование сайтов в многопоточном режиме (BFS обход)
* **Умный поиск:** Полнотекстовый поиск с использованием морфологии (бежит/бегать/бег → одна лемма)
* **Ранжирование:** Расчет относительной релевантности на основе весов лемм (TF-IDF подход)
* **Удобный UI:** Веб-интерфейс для управления процессом и просмотра результатов со сниппетами

## 🌟Технологический стек
* **Java 17+** - основной язык разработки(рекомендуется OpenJDK 17)
* **Spring Boot** - фреймворк для backend
* **Hibernate/JPA** - работа с базой данных
* **MySQL 8.0+** - система управления базами данных(обязательно с поддержкой UTF-8)
* **Lucene Morphology** - морфологический анализ
* **HTML/CSS/JavaScript** - фронтенд интерфейс
* **Maven 3.6+** - управление зависимостями
* **Jsoup	1.16** - парсинг HTML

## 🌟Технические особенности:
* Морфологический анализ русского и английского языков
* Исключение стоп-слов и служебных частей речи
* Расчет TF-IDF для ранжирования результатов
* Постраничный вывод результатов поиска
* Параллельная индексация нескольких сайтов

## 📁 Структура проекта
````text
search-engine/
├── src/main/java/searchengine/
│   ├── Application.java        # Основной класс запуска
│   ├── controllers/           # REST API контроллеры
│   │   ├── ApiController.java
│   │   └── DefaultController,java
│   ├── services/             # Бизнес-логика
│   │   ├── StatisticsService.java
│   │   ├── SiteService.java
│   │   ├── SearchService.java
│   │   ├── LemmaService.java
│   │   └── IndexingService.java
│   ├── models/               # Сущности БД
│   │   ├── SiteEntity.java
│   │   ├── PageEntity.java
│   │   ├── LemmaEntity.java
│   │   └── IndexEntity.java
│   ├── repository/           # Репозитории Spring Data
│   │   ├── SiteRepository.java
│   │   ├── PageRepository.java
│   │   ├── LemmaRepository.java
│   │   └── IndexRepository.java
│   └── dto/                  # Data Transfer Objects
│       └── ...
├── src/main/resources/
│   ├── application.yaml      # Конфигурация
│   ├── static/              # Веб-ресурсы
│   └── templates/           # HTML шаблоны
└── pom.xml                  # Maven зависимости
````

## 📊 Структура базы данных
````sql
-- Основные таблицы:
site           # Сайты (url, имя, статус)
├── id (INT, PK)
├── url (VARCHAR(255))        -- https://example.com
├── name (VARCHAR(255))       -- Отображаемое имя
├── status (ENUM)             -- INDEXING/INDEXED/FAILED
└── status_time (DATETIME)    -- Время последнего изменения

page           # Страницы сайтов
├── id (INT, PK)
├── site_id (INT, FK)         -- Ссылка на сайт
├── path (VARCHAR(500))       -- Относительный путь /about
├── code (INT)                -- HTTP-код ответа (200, 404, ...)
├── content_html (MEDIUMTEXT) -- Полный HTML
└── content_text (MEDIUMTEXT) -- Очищенный текст

lemma          # Леммы (нормальные формы слов)
├── id (INT, PK)
├── site_id (INT, FK)         -- Принадлежность сайту
├── lemma (VARCHAR(255))      -- Сама лемма ("книга")
└── frequency (INT)           -- Частота на сайте

index_table    # Индекс (связь страниц и лемм)
├── id (INT, PK)
├── page_id (INT, FK)         -- Страница
├── lemma_id (INT, FK)        -- Лемма
└── rank_count (FLOAT)        -- Вес леммы на странице
````
## 🔧 API Endpoints
Основные методы:
![мет](https://github.com/user-attachments/assets/89f46748-7d77-4927-97f8-3cd181cc6979)

## 🔢 Порядок выполнения действий для работы с программой:
* Клонировать репозиторий
* Создать базу данных MySQL:
* Настроить подключение к БД в application.yaml
* Собрать и запустить проект
* Открыть веб-интерфейс: http://localhost:8080

## ⚙️ Конфигурация приложения
Файл application.yaml - основные настройки:
````yaml
# crawler: настройки сканера сайтов
crawler:
  user-agent: "HeliontSearchBot/1.0"    # Имя бота для HTTP-заголовков
  referrer: "https://www.google.com"    # Реферер для обхода ограничений
  delay:
    min: 500      # МИНИМАЛЬНАЯ задержка между запросами (мс) - чтобы не блокировали
    max: 5000     # МАКСИМАЛЬНАЯ задержка (случайная)
  timeout: 5000   # Таймаут HTTP-запросов (мс)
  batch_pause: 3000  # Пауза между партиями страниц (мс)

# spring: настройки Spring Boot
spring:
  datasource:
    # ⚠️ ВАЖНО: замените <ваш пароль> на пароль от MySQL
    url: jdbc:mysql://localhost:3306/search_engine_test?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root                      # Логин MySQL
    password: <ваш пароль>              # ⬅️ ЗАМЕНИТЕ ЭТО!
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      # 🛠️ ВАЖНАЯ НАСТРОЙКА ddl-auto:
      #   • "create" - СОЗДАЕТ таблицы ЗАНОВО (удаляет старые!)
      #   • "update" - ОБНОВЛЯЕТ структуру (сохраняет данные)
      #   • "validate" - только проверяет схему
      #   • "create-drop" - создает при старте, удаляет при остановке
      ddl-auto: update                  # Используйте "create" при ПЕРВОМ запуске!
    
    show-sql: false                     # true - показывать SQL в логах (для отладки)
    
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true                # Красивое форматирование SQL

# server: настройки веб-сервера
server:
  port: 8080                           # Порт веб-интерфейса (http://localhost:8080)
````

## Настройка базы данных
```sql
-- 1. Создайте базу данных (обязательно UTF-8!)
CREATE DATABASE search_engine CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 2. Проверьте создание
SHOW CREATE DATABASE search_engine;
````

## 🌐 Веб-интерфейс
После запуска откройте в браузере http://localhost:8080

## 📊 Вкладка Dashboard (Статистика)
Количество сайтов, страниц, лемм

Статус индексации:

* 🟢 INDEXED - готово
* 🟡 INDEXING - в процессе
* 🔴 FAILED - ошибка

Список проиндексированных сайтов

## ⚙️ Вкладка Management (Управление)
Запуск индексации
````yaml
Start Indexing:
  • All sites - индексация ВСЕХ сайтов сразу
  • Конкретный сайт - выберите из списка для индексации
  
Add Site:
  • Site name: Отображаемое имя (например: "Playback")
  • Site URL: Полный адрес (например: "https://www.playback.ru")

Delete Site:
  • Выберите сайт → Delete - удаляет сайт
````

## 🔍 Вкладка Search
````text
1. Выберите сайт: "All sites" или конкретный
2. Введите запрос: любая фраза или слово
3. Нажмите SEARCH или Enter
Получаем результаты поиска с заголовками, сниппетами и релевантностью
````

## 🧠 Алгоритм поиска
Пошаговый процесс
````text
ПОИСКОВЫЙ ЗАПРОС "книга и собака"
    │
    ▼
1. ТОКЕНИЗАЦИЯ
   ["книга", "и", "собака"]
    │
    ▼
2. ЛЕММАТИЗАЦИЯ (удаление служебных слов)
   ["книга", "собака"] ← "и" удалено
    │
    ▼
3. ПОИСК ЛЕММ В БАЗЕ
   - Ищем "книга" в таблице lemma
   - Ищем "собака" в таблице lemma
    │
    ▼
4. ФИЛЬТРАЦИЯ (>80% страниц игнорируется)
   - Если "книга" на 90% страниц → игнорируется
   - Если "собака" на 30% страниц → остается
    │
    ▼
5. СОРТИРОВКА ЛЕММ (от редких к частым)
   ["собака"] → ["книга"]
    │
    ▼
6. ПОИСК СТРАНИЦ
   - Находим все страницы с "собака"
   - Фильтруем те, где есть "книга"
    │
    ▼
7. РАСЧЁТ РЕЛЕВАНТНОСТИ
   Относительная релевантность = R_абс / R_макс
   где R_абс = сумма rank всех найденных лемм на странице
   R_отн = (ранг_страницы) / (макс_ранг_в_результатах)
    │
    ▼
8. ГЕНЕРАЦИЯ СНИППЕТОВ
   Текст с <b>выделенными</b> словами
    │
    ▼
9. СОРТИРОВКА РЕЗУЛЬТАТОВ
   По убыванию релевантности
    │
    ▼
   📄 РЕЗУЛЬТАТЫ ПОИСКА
````

## 💬 Формат ответа поиска
````json
{
  "result": true,
  "count": 42,
  "data": [
    {
      "site": "https://playback.ru",
      "siteName": "Playback",
      "uri": "/examples/javascript",
      "title": "TodoMVC • JavaScript",
      "snippet": "Пример реализации <b>JavaScript</b> для управления задачами...",
      "relevance": 0.75
    }
  ]
}
````
## ⚡ Особенности:
* Поддержка 🇷🇺 русского и 🇬🇧 английского языков
* Использование LuceneMorphology для выделения лемм
* Игнорирование стоп-слов ("и", "или", "the", "and")
* Исключение предлогов, союзов, частиц и междометий
* Морфологический анализ (бежит/бегать/бег → "бег")

## 🐛 Отладка
### Полезные SQL-запросы:
````sql
-- Проверка наличия данных
SELECT 
    (SELECT COUNT(*) FROM site) as sites,
    (SELECT COUNT(*) FROM page) as pages,
    (SELECT COUNT(*) FROM lemma) as lemmas,
    (SELECT COUNT(*) FROM index_table) as indexes;

-- Поиск конкретной леммы
SELECT s.name, l.lemma, l.frequency, COUNT(i.id) as indexes
FROM lemma l
JOIN site s ON l.site_id = s.id
LEFT JOIN index_table i ON l.id = i.lemma_id
WHERE l.lemma = 'read'
GROUP BY l.id;

-- Статистика по сайту
SELECT 
    s.name,
    s.status,
    COUNT(DISTINCT p.id) as pages,
    COUNT(DISTINCT l.id) as lemmas,
    COUNT(DISTINCT i.id) as indexes
FROM site s
LEFT JOIN page p ON s.id = p.site_id
LEFT JOIN lemma l ON s.id = l.site_id
LEFT JOIN index_table i ON l.id = i.lemma_id
GROUP BY s.id;
````
## 🚨 Решение проблем
### Частые проблемы и решения:
![prbl](https://github.com/user-attachments/assets/9a74150f-59ea-404a-a470-b8850aee12db)

## 📚 Дополнительные материалы
### Для разработки:
* [Spring Boot Documentation](https://spring.io/projects/spring-boot)
* [SQL 8.0 Reference](https://dev.mysql.com/doc/refman/8.0/en/)
* [Jsoup](https://jsoup.org/cookbook/input/load-document-from-url)
* [Морфологический разбор слов](https://morphologyonline.ru)

## ✨Планы по развитию
### Будущие улучшения:
* Добавление синонимов в поиск
* Улучшение алгоритма сниппетов
* Поддержка дополнительных языков
* Мобильная версия интерфейса
* API для интеграции с другими системами

## 📝 Лицензия
### Отказ от ответственности:
````text
Данное программное обеспечение предоставляется «как есть» (as is), без каких-либо гарантий. Автор не несет ответственности за любые прямые или косвенные убытки, возникшие в результате использования данного кода в любых целях. Вы используете этот код на свой страх и риск.
````

## 👨‍💻Контакты
Для вопросов и предложений по проекту обращайтесь к разработчику.

Последнее обновление: декабрь 2025
